apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/ssathi/spring-service1:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/ssathi/spring-service1:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: spring-service1
        chartPath: charts/spring-service1
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/ssathi/spring-service1'
          image.tag: '{{.DIGEST_HEX}}'
